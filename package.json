{
  "name": "awesome-things",
  "version": "1.0.0",
  "description": "Comprehensive development guides and best practices repository",
  "private": true,
  "scripts": {
    "lint:markdown": "markdownlint '**/*.md' --ignore node_modules --ignore .git --config .markdownlint.json",
    "lint:markdown:fix": "markdownlint '**/*.md' --ignore node_modules --ignore .git --config .markdownlint.json --fix"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.38.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
